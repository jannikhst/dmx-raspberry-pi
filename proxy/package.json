{
  "name": "proxy",
  "version": "1.0.0",
  "description": "websocket proxy for pipong",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dist/node app.js",
    "build": "tsc -p ."
  },
  "author": "Jannik Hurst",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  }
}
